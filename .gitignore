node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Local JSON "database" (runtime data)
users.json

# Claude Code worktrees (nested git repos, local-only)
.claude/worktrees/
.claude/scheduled_tasks.lock

# Sanity Studio lives in its own repo at E:/Business/Claude/Portfolio/studio
# Anchored to repo root so it does NOT swallow src/components/studio/ (AI Studio UI).
/studio/

# #12 identify test fixtures (dev-only, large binaries)
scripts/identify-test-images/
scripts/aivision/__pycache__/
