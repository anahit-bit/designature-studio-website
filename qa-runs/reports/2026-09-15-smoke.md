# QA report: smoke (first run)

- **Run date:** 2026-09-15 16:40 Asia/Yerevan
- **Environment:** local (`http://localhost:3000`, `npm run dev`, no Postgres, no outbound network). Production (`https://www.designature.studio`) was not reachable from the sandbox that ran this pass; rerun `/qa-smoke` from a machine with network access to confirm on production.
- **Profile:** smoke, widened to every public route in `qa-config.yml` on both viewports because this is the baseline run.
- **Build ID:** n/a on the dev server (Vite serves unhashed modules). Capture `/assets/index-*.js` on production.
- **Pre-flight:** Chromium launched via `QA_CHROME_PATH=/opt/pw-browsers/chromium`; Playwright MCP was not available, so the pass was driven by two Playwright scripts kept in `qa-runs/state/` (gitignored) plus the helper CLI (`batch-fingerprint`, `axe`, `vitals`, `screenshot`).
- **Routes covered:** 16 of 20 configured routes, desktop and mobile.
- **Findings:** 0 critical, 2 high, 3 medium, 2 low.
- **Diff vs prior:** first run, no prior report.

## Run status

**Run is complete for what the local environment can show.** Four routes were not reached: `/portfolio/25` (duplicate of the `/portfolio/32` case), `/booking/confirmed` and `/booking/failed` (need a real booking token), `/account` and `/admin` (configured `auth_required`). Everything that depends on the network (Sanity journal posts, Cloudinary images, Google Fonts, Skimlinks, analytics, Calendly and Google Calendar slots) could not be judged and is listed under "Not testable locally", not as findings.

## Coverage report

| Section | Routes in config | Visited desktop | Visited mobile | Skipped (reason) |
| --- | --- | --- | --- | --- |
| home | 4 | 4 | 4 | |
| portfolio | 3 | 2 | 2 | `/portfolio/25` (same template as `/portfolio/32`) |
| ai-studio | 3 | 3 | 3 | |
| pricing | 1 | 1 | 1 | |
| consultation | 3 | 1 | 1 | `/booking/confirmed`, `/booking/failed` (need a booking token) |
| journal | 1 | 1 | 1 | posts list empty locally (CMS unreachable) |
| legal | 3 | 3 | 3 | |
| account | 2 | 0 | 0 | `/account`, `/admin`: skipped, auth required (configured) |
| unknown route | 1 | 1 | 1 | `/this-does-not-exist-zzz` and `/faq/this-does-not-exist-zzz` |

Locales: en (only). Viewports: desktop 1920×1080, mobile 390×844.

## Fixed pass checklist

| Check | Result |
| --- | --- |
| Every section's main route renders with HTTP 200 | pass, all 16 routes |
| No console errors of our own on any route | pass, apart from F2 (`tailwind is not defined`, caused by the blocked CDN) |
| Primary interaction, home: nav and the four AI Studio cards navigate | pass (`/portfolio`, `/services`, `/journal`, `/studio`, `/pricing`, `/ai-concepts#quiz`, `#vision`, `#shopping`, `#audit`) |
| Primary interaction, faq: expand two questions | pass, 37 question buttons, 2 expanded with answer text |
| Primary interaction, pricing: toggle interval | not applicable locally; no monthly/annual switch rendered (credit model is off, prices shown $0, $39, $129, $249, $49, $99); 3 buy style buttons present, not clicked |
| Primary interaction, portfolio: open first project | pass via `/portfolio/32` (H1 "Two Story Living Room", 12 images); see F6 for how cards are built |
| Primary interaction, ai-studio: start the Style Quiz | pass, "Four questions, about a minute" step rendered, 26 buttons; no generation triggered |
| Primary interaction, consultation: slot picker | not testable locally (slots API needs Google Calendar) |
| 404 page renders for an unknown route | **fail**, see F1 |
| Mobile: renders, no horizontal scroll, mobile nav present | pass on all routes (scrollWidth equals clientWidth everywhere; burger opens 6 links) |
| Resize desktop to mobile without reload | pass, no horizontal scroll after resize |
| `<html lang>` matches content | pass (`en`) |

## Findings summary

| ID | Route | Severity | Category | Summary |
| --- | --- | --- | --- | --- |
| F1 | global | high | broken-flow | Unknown URLs silently redirect to the home page; there is no 404 page |
| F2 | global | high | performance | Production styling depends on the Tailwind Play CDN script; when it is blocked, headings lose their fonts and every page throws `tailwind is not defined` |
| F3 | global | medium | accessibility | Footer Facebook and Instagram icon links have no accessible name |
| F4 | `/`, `/journal`, `/ai-concepts` | medium | accessibility | Text below the 4.5:1 contrast ratio: home eyebrow labels, journal breadcrumb, "Showing 4 live now" |
| F5 | global, `/ai-concepts` | medium | accessibility | Missing or duplicated landmarks and heading order: no `<main>` on 6 routes, two `<header>` elements on `/ai-concepts`, footer `h4` on every page |
| F6 | `/portfolio`, `/` | low | accessibility | Portfolio cards are buttons that navigate with JavaScript, so projects cannot be opened in a new tab and the listing exposes no links to project pages |
| F7 | `/journal` | low | broken-flow | When the CMS request fails the page shows "Loading the journal…" forever instead of an error state |

## Findings

```json
{
  "id": "F1",
  "route": "global (tested /this-does-not-exist-zzz and /faq/this-does-not-exist-zzz)",
  "locale": "en",
  "viewport": "desktop|mobile",
  "severity": "high",
  "category": "broken-flow",
  "summary": "Unknown URLs redirect to the home page; no 404 page exists.",
  "detail": "src/App.tsx line 201 declares `<Route path=\"*\" element={<Navigate to=\"/\" replace />} />`. A visitor with a mistyped or stale link lands on the home page with no message, and the URL bar is rewritten to `/`. Production marks unknown paths noindex in the server render (SEO-NOTES.md), which contains the SEO cost, but the visitor experience is the same. The skill's fixed pass requires a 404 page, so severity is high by rubric; the product impact is moderate.",
  "evidence": "Local run: both URLs returned 200 and rendered H1 'Bring us a space. We'll bring it to life.' Screenshot not needed; the App.tsx line is the proof.",
  "reproduction": "1. Open /anything-that-does-not-exist. 2. Observe redirect to / with no notice."
}
```

```json
{
  "id": "F2",
  "route": "global",
  "locale": "en",
  "viewport": "desktop|mobile",
  "severity": "high",
  "category": "performance",
  "summary": "index.html loads the Tailwind Play CDN and defines the brand tokens there; the compiled Tailwind 4 build does not carry them.",
  "detail": "index.html line 58 loads https://cdn.tailwindcss.com and lines 63 to 80 set `tailwind.config` with `font-display` (Cormorant Garamond), `font-body` (Montserrat), `tracking-architectural` and the `shake` animation. src/index.css has no `@theme` block, so those classes exist only if the CDN script runs. The site is already built with Tailwind 4 through @tailwindcss/vite, so every page ships two Tailwind engines, and the Play CDN is documented by Tailwind as not for production. When the CDN is blocked (ad blockers, corporate networks, offline, this sandbox) every page throws `tailwind is not defined` 8 times and headings fall back to the default sans font. Fix: move the four tokens into an `@theme` block in src/index.css, delete the script and the inline config, then compare screenshots.",
  "evidence": "Page errors captured on every route: 'tailwind is not defined' x8. index.html lines 58 to 80. `grep @theme src/index.css` returns nothing.",
  "reproduction": "1. Block cdn.tailwindcss.com in the browser (or go offline after the first paint). 2. Reload any page. 3. Headings render in the fallback font and the console shows the error."
}
```

```json
{
  "id": "F3",
  "route": "global (footer)",
  "locale": "en",
  "viewport": "desktop|mobile",
  "severity": "medium",
  "category": "accessibility",
  "summary": "Footer social icon links have no discernible text (axe link-name, serious).",
  "detail": "src/components/Footer.tsx lines 222 and 231: the Facebook and Instagram anchors contain only an SVG and carry no aria-label or visually hidden text. Screen readers announce 'link'. Fix: add aria-label=\"Designature on Facebook\" and \"Designature on Instagram\".",
  "evidence": "axe on /, /faq, /consultation, /journal, /ai-concepts, /portfolio, /pricing: link-name, 2 nodes each.",
  "reproduction": "1. Run axe on any page. 2. Or tab to the footer icons with a screen reader on."
}
```

```json
{
  "id": "F4",
  "route": "/, /journal, /ai-concepts",
  "locale": "en",
  "viewport": "desktop",
  "severity": "medium",
  "category": "accessibility",
  "summary": "Text below 4.5:1 contrast (axe color-contrast, serious): 3 nodes on /, 4 on /journal, 3 on /ai-concepts.",
  "detail": "/: eyebrow labels `text-[#0047AB]` at 11 to 12 px on the section background. /journal: breadcrumb links (`hover:text-black`, 'Designature Studio'). /ai-concepts: 'Showing 4 live now' in `text-white/40`. Small uppercase labels are exactly where low contrast hurts most. Fix: darken the blue or enlarge the label on /, use text-black/70 for the breadcrumb, text-white/70 for the counter.",
  "evidence": "axe color-contrast samples in the run output.",
  "reproduction": "1. Run axe on /, /journal, /ai-concepts. 2. Inspect the sampled nodes."
}
```

```json
{
  "id": "F5",
  "route": "global; /ai-concepts",
  "locale": "en",
  "viewport": "desktop",
  "severity": "medium",
  "category": "accessibility",
  "summary": "Landmark structure is inconsistent: no <main> on /pricing, /faq, /consultation, /journal, /portfolio, /ai-concepts; two <header> elements on /ai-concepts; footer h4 breaks heading order on every page.",
  "detail": "/, /studio and /services wrap content in <main>; the other pages do not, so axe reports landmark-one-main and 8 to 29 nodes outside any landmark. /ai-concepts renders two <header> elements (the nav text appears twice in the accessibility tree: PORTFOLIO SERVICES JOURNAL... repeated), reported as landmark-no-duplicate-banner. The footer 'Studio' column header is an h4 following h1/h2, and /pricing uses an h4 for 'How credits work'. Fix: one <main> per page in App.tsx layout, one header on /ai-concepts, footer column titles as <p> or h2 with visually small styling.",
  "evidence": "DOM counts per route: headers 2 and mains 0 on /ai-concepts; mains 0 on /pricing, /portfolio, /faq, /consultation, /journal. axe heading-order on every page.",
  "reproduction": "1. Open /ai-concepts. 2. document.querySelectorAll('header').length returns 2. 3. Run axe."
}
```

```json
{
  "id": "F6",
  "route": "/portfolio, /",
  "locale": "en",
  "viewport": "desktop|mobile",
  "severity": "low",
  "category": "accessibility",
  "summary": "Portfolio cards are <button> elements that navigate via JavaScript; the listing exposes zero links to project pages.",
  "detail": "src/components/Portfolio.tsx line 42: onClick navigateTo('project-detail', project.id). Keyboard users can reach the buttons, but nobody can open a project in a new tab or copy its link from the card, and crawlers see no anchor from the listing to /portfolio/:id (discovery today relies on the sitemap). Fix: render each card as <Link to=\"/portfolio/:id\"> and keep the same styling.",
  "evidence": "document.querySelectorAll('a[href^=\"/portfolio/\"]').length is 0 on /portfolio and on /; the visible cards are BUTTON elements.",
  "reproduction": "1. Open /portfolio. 2. Middle click a card: nothing opens. 3. Hover: no URL in the status bar."
}
```

```json
{
  "id": "F7",
  "route": "/journal",
  "locale": "en",
  "viewport": "desktop|mobile",
  "severity": "low",
  "category": "broken-flow",
  "summary": "If the CMS request fails, /journal shows 'Loading the journal…' indefinitely.",
  "detail": "src/components/JournalPage.tsx: the fetch failure is caught at line 223 without setting an error state, and the loading copy at line 390 stays on screen. Seen locally because Sanity is unreachable from the sandbox; on production it appears only during a Sanity outage. Fix: an error state with a retry button and a link to the home page.",
  "evidence": "Body text on /journal after 1.5 s: 'Loading the journal…', 0 post links.",
  "reproduction": "1. Block *.sanity.io. 2. Open /journal. 3. Wait: the loading line never changes."
}
```

## Not testable locally

- Canonical link, per route `<title>` and JSON LD: injected by the production server render (`server/seo/render.ts`), absent on the Vite dev server by design. Check on production.
- Cloudinary images, Google Fonts, Skimlinks, analytics: blocked by the sandbox network. Not findings.
- Journal posts and categories: Sanity unreachable.
- Consultation slot picker: needs the calendar API.
- Payments, credits, subscriptions, AI generation: never exercised by the QA agent by rule.

## Web vitals (local, mobile, home)

LCP 796 ms, FCP 796 ms, CLS 0, TTFB 7 ms. All "good", but hero images did not load locally, so production numbers will differ. Rerun the helper on production for a real baseline.

## Out of scope observations

- The home hero has no single call to action link; the four AI Studio cards act as the CTA. Fine, but the fixed pass needed a definition, now written in `qa-config.yml`.
- Retailer logos on /retail use `alt=""` (37 images). Correct for decorative images placed next to the retailer name.
- Four H1s use a `<br>` mid sentence ("A focused 45 minutes / on your space"). Screen readers handle this correctly. No action.
